[workspace]
members = [
    "sentinel-agent",
    "sentinel-ebpf",
    "sentinel-ebpf-common",
    "xtask",
]
resolver = "2"

[profile.release]
lto = true
debug = false
strip = true
opt-level = 3
panic = "abort"
